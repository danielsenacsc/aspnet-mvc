@model IEnumerable<CadeMeuMedico.Models.Cidade>

@{
    ViewBag.Title = "Lista";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Criar nova", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IDCidade }) |
            @Html.ActionLink("Visualizar", "Details", new { id=item.IDCidade }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.IDCidade })
        </td>
    </tr>
}

</table>
